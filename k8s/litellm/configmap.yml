apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
  namespace: ai-lab
  labels:
    app: litellm
data:
  config.yml: |
    # This is mounted from gateway/litellm-config.yml via the deploy playbook.
    # The actual content is injected by:
    #   kubectl create configmap litellm-config -n ai-lab --from-file=config.yml=gateway/litellm-config.yml --dry-run=client -o yaml | kubectl apply -f -
    # See ansible/playbooks/deploy.yml for automation.